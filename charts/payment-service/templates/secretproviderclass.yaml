{{- if .Values.secretProviderClass.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: payment-service
  namespace: {{ .Release.Namespace }}
  # No app.kubernetes.io/name or part-of here — the existing Kustomize
  # overlay's SecretProviderClass never declared metadata.labels at all;
  # only the overlay's own homeease.io/cloud + homeease.io/environment
  # pair reaches it. Matching that exactly.
  {{- with .Values.extraLabels }}
  labels:
    {{- range $k, $v := . }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
  {{- end }}
spec:
  provider: azure
  parameters:
    clientID: {{ .Values.workloadIdentity.clientId | quote }}
    keyvaultName: {{ .Values.keyVault.name | quote }}
    tenantId: {{ .Values.keyVault.tenantId | quote }}
    # razorpay-webhook-secret deliberately not in .Values.secretProviderClass.objects
    # by default — the CSI driver fails the ENTIRE mount if any one
    # requested object doesn't exist in Key Vault, and Razorpay webhooks
    # aren't configured yet.
    objects: |
      array:
        {{- range .Values.secretProviderClass.objects }}
        - |
          objectName: {{ .keyVaultObject }}
          objectType: secret
        {{- end }}
  secretObjects:
    - secretName: {{ .Values.secretProviderClass.secretName }}
      type: Opaque
      data:
        {{- range .Values.secretProviderClass.objects }}
        - objectName: {{ .keyVaultObject }}
          key: {{ .envKey }}
        {{- end }}
{{- end }}
