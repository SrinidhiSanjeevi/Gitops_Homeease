# Standalone demo values. No ingress.host override needed — this chart
# has no Ingress template at all (internal-only by design, unchanged
# here). Otherwise identical to values-azure-dev.yaml; see
# charts/backend/values-demo.yaml for the workloadIdentity/
# secretProviderClass federation caveat that applies here too.
image:
  repository: acrhomeeasedev01.azurecr.io/homeease/payment-service
  tag: 8af8212190cc

workloadIdentity:
  enabled: true
  clientId: "906080fd-636f-4c49-ab58-f7da81381014"

keyVault:
  name: kv-homeease-dev-hs02
  tenantId: "268c9f5b-bb6f-44e8-b872-ea399b1998c2"

secretProviderClass:
  enabled: true

extraLabels:
  homeease.io/environment: demo
  homeease.io/cloud: azure
