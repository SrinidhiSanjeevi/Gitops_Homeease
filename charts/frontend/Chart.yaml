apiVersion: v2
name: frontend
description: HomeEase frontend (React SPA served by nginx-unprivileged) — Helm equivalent of apps/frontend Kustomize base+overlay
type: application
version: 0.1.0
appVersion: "1.0.0"
