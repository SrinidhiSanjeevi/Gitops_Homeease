# Direct Helm equivalent of apps/frontend/overlays/azure/dev/kustomization.yaml.
image:
  repository: acrhomeeasedev01.azurecr.io/homeease/frontend
  tag: 5c7140b103f5
ingress:
  host: home.20-207-75-39.nip.io
  sslRedirect: false
  extraHostlessRule: true
extraLabels:
  homeease.io/environment: dev
  homeease.io/cloud: azure
# Free Trial subscription: hard 4-vCPU cap, so DEV is one 2-vCPU node.
# Only the CPU *request* is lowered so the whole stack schedules; limits
# come from values.yaml unchanged, so the pod can still burst.
resources:
  requests:
    cpu: 25m
