apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: homeease
  annotations:
    # No-op until platform/cert-manager is actually installed (still a
    # placeholder per platform/README.md).
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - app.dev.example.com # overwritten per cloud/environment, see overlays/*/kustomization.yaml
      secretName: frontend-tls
  rules:
    - host: app.dev.example.com # overwritten per cloud/environment, see overlays/*/kustomization.yaml
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  name: http
